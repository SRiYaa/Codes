#include <stdio.h>
int main()
{
  int n, c, d, pos, t;
  printf("Enter number of elements\n");
  scanf("%d", &n);
  int arr[n];
  printf("Enter %d integers\n", n);
  for (c = 0; c < n; c++)
  scanf("%d", &arr[c]);
}

  pivot()
  {
  	pi=arr[0];
  	for(int i=0;i<n;i++)
  	{
  		if(arr[i+1]>pi)
  		if(arr[n-i-1]<pi)		
	}
  }

